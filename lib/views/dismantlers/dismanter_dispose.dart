import 'package:flutter/material.dart';

class DismantlerDispose extends StatelessWidget {
  const DismantlerDispose({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
